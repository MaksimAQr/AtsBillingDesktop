<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:ATS_Desktop.ViewModels"
             x:Class="ATS_Desktop.Views.Components.ImportExportPanel"
             x:Name="Self"
             x:DataType="vm:MainWindowViewModel">
    
    <Border Background="#2C3E50"
            CornerRadius="8"
            Padding="10">
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
  
                <Border Grid.Column="0"
                    Background="#34495E"
                    CornerRadius="5"
                    Padding="10"
                    Margin="0,0,10,0"
                    Width="120">
            <TextBlock Text="{Binding ImportExportVM.Status}" 
                       VerticalAlignment="Center"
                       Foreground="#BDC3C7"
                       FontSize="10"
                       MaxWidth="100"
                       TextWrapping="Wrap"
                       ToolTip.Tip="{Binding ImportExportVM.Status}"/>
            </Border>          

                        <Border Grid.Column="1"
                    Background="#34495E" 
                    CornerRadius="5" 
                    Padding="15">
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0"
                               Text="Импорт тарифов" 
                               Foreground="White"
                               FontWeight="Bold"
                               FontSize="14"
                               Margin="0,0,0,10"/>
                    
                    <!-- Поле пути импорта -->
                    <Grid Grid.Row="1" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBox Grid.Column="0"
                                 Text="{Binding ImportExportVM.ImportFilePath, Mode=TwoWay}"
                                 Watermark="Введите путь к JSON файлу..."
                                 Name="ImportPathBox"
                                 Margin="0,0,10,0">
                            <TextBox.Styles>
                                <Style Selector="TextBox:error">
                                    <Setter Property="BorderBrush" Value="#E74C3C"/>
                                </Style>
                            </TextBox.Styles>
                        </TextBox>
                        
                   <Button
                        Grid.Column="1" 
                        Content="📥" 
                        Command="{Binding #Self.ImportCommand}"
                        ToolTip.Tip="Импортировать тарифы из JSON файла"
                        Background="#3498DB"
                        Foreground="White"
                        Padding="8,5"
                        FontSize="12"
                        Width="30"/>
                    </Grid>
                    
                    <!-- Сообщение об ошибке импорта -->
                    <TextBlock Grid.Row="2"
                               Text="{Binding ImportExportVM.ImportError}"
                               Foreground="#E74C3C"
                               FontSize="12"
                               TextWrapping="Wrap"
                               IsVisible="{Binding ImportExportVM.HasImportError}"/>
                </Grid>
            </Border>

                        <Border Grid.Column="3"
                    Background="#34495E" 
                    CornerRadius="5" 
                    Padding="15">
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0"
                               Text="Экспорт тарифов" 
                               Foreground="White"
                               FontWeight="Bold"
                               FontSize="14"
                               Margin="0,0,0,10"/>
                    
                    <!-- Поле пути экспорта -->
                    <Grid Grid.Row="1" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBox Grid.Column="0"
                                 Text="{Binding ImportExportVM.ExportFilePath, Mode=TwoWay}"
                                 Watermark="Введите путь для JSON файла..."
                                 Name="ExportPathBox"
                                 Margin="0,0,10,0">
                            <TextBox.Styles>
                                <Style Selector="TextBox:error">
                                    <Setter Property="BorderBrush" Value="#E74C3C"/>
                                </Style>
                            </TextBox.Styles>
                        </TextBox>
                        
                        <Button Grid.Column="1"
                                Content="📤" 
                                Command="{Binding #Self.ExportCommand}"
                                ToolTip.Tip="Экспортировать тарифы в JSON файл"
                                Background="#2ECC71"
                                Foreground="White"
                                Padding="8,5"
                                FontSize="12"
                                Width="30"/>
                    </Grid>
                    
                    <!-- Сообщение об ошибке экспорта -->
                    <TextBlock Grid.Row="2"
                               Text="{Binding ImportExportVM.ExportError}"
                               Foreground="#E74C3C"
                               FontSize="12"
                               TextWrapping="Wrap"
                               IsVisible="{Binding ImportExportVM.HasExportError}"/>
                </Grid>
            </Border>
            
        </Grid>
    </Border>
</UserControl>